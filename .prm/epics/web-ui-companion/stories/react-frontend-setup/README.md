# React Frontend Setup

Initialize React project in `web/` directory with build pipeline.

## Scope
- Vite + React + TypeScript project scaffolding
- Build outputs to `web/dist/`
- Go embed integration in `internal/web/embed.go`
- CSS framework choice (Tailwind CSS or CSS modules)
- API client utility
- React Router for client-side navigation